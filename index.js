/**
 * Velron CFW – Rules Bot
 * Full File | discord.js v14
 */

const {
  Client,
  GatewayIntentBits,
  EmbedBuilder,
  ActionRowBuilder,
  StringSelectMenuBuilder
} = require('discord.js');

// ===============================
// Client
// ===============================
const client = new Client({
  intents: [GatewayIntentBits.Guilds]
});

// ===============================
// Ready
// ===============================
client.once('ready', async () => {
  console.log(`✅ Logged in as ${client.user.tag}`);

  // 🟡 آيدي الروم
  const channel = await client.channels.fetch('1457506347456270468');

  const mainEmbed = new EmbedBuilder()
    .setTitle('Velron CFW')
    .setDescription(
      '**— قوانين سيرفر فلرون —**\n\n' +
      'يرجى اختيار القسم لعرض القوانين\n' +
      'عدم الالتزام بهذه القوانين يعرضك للمحاسبة'
    )
    .setColor(0x6a0dad)
    .setImage(
      'https://cdn.discordapp.com/attachments/1437919610362925217/1457539284255903784/Cfw.png'
    );

  const menu = new StringSelectMenuBuilder()
    .setCustomId('rules_categories')
    .setPlaceholder('اختر قسم القوانين')
    .addOptions([
      { label: '📘 القوانين الرئيسية', value: 'general' },
      { label: '🚓 قوانين الإجرام', value: 'crime' },
      { label: '🛡️ المناطق الآمنة', value: 'safe' },
      { label: '📝 آلية الشكاوي', value: 'reports' },
      { label: '🛒 قوانين المتجر', value: 'store' },
      { label: '💬 قوانين الدسكورد', value: 'discord' }
    ]);

  const row = new ActionRowBuilder().addComponents(menu);

  await channel.send({
    embeds: [mainEmbed],
    components: [row]
  });
});

// ===============================
// Interaction
// ===============================
client.on('interactionCreate', async interaction => {
  if (!interaction.isStringSelectMenu()) return;
  if (interaction.customId !== 'rules_categories') return;

  await interaction.deferReply({ ephemeral: true });

  let embed;

  if (interaction.values[0] === 'general') {
    embed = new EmbedBuilder()
      .setTitle('📘 القوانين الرئيسية')
      .setColor(0x2b2d31)
      .setDescription(
`1. يمنع التواصل الغير شرعي (Meta Gaming).
2. يمنع استخدام المركبة كسلاح (VDM).
3. يمنع القتل العشوائي (RDM).
4. تقمص الشخصية إلزامي بالأقوال والأفعال.
5. يمنع إهانة الشخص المسقط.
6. يمنع تقليد ملابس الشرطة أو الإسعاف.
7. يمنع الخروج عن الرول بلاي لأي سبب.
8. /report مخصص للمخالفات الخارجة عن الرول بلاي.
9. التحدث عن الألم فقط عند الإسقاط.
10. القلتشات أو الهاك = باند نهائي.
11. تحريك الجثة للمساعدة فقط.
12. يمنع أي اتفاق خارج الرول بلاي.
13. التصوير مع الصوت إلزامي (20 دقيقة).
14. يمنع التدخل في أي سيناريو قائم.
15. يمنع لبس الخوذة أثناء الطلق الناري.
16. يمنع الرجوع لموقع الحالة.
17. يمنع ربط الشخصيات.
18. الشخصنة ممنوعة بجميع أشكالها.
19. يجب تقدير حياتك عند التهديد.
20. يمنع الخروج من السيرفر أثناء السيناريو.`
      );
  } else if (interaction.values[0] === 'crime') {
    embed = new EmbedBuilder()
      .setTitle('🚓 قوانين الإجرام')
      .setColor(0x2b2d31)
      .setDescription(
`1. يمنع الإجرام بدون سيناريو واضح.
2. الالتزام بعدد المجرمين المحدد.
3. يمنع الإجرام العشوائي.
4. يمنع خطف أو التعرض للمسعفين.
5. يمنع سرقة أغراض الشرطة أو الإسعاف.
6. يمنع التربص بالموظفين الحكوميين.
7. يمنع الإجرام داخل المناطق الآمنة.
8. يمنع بيع أو الإعلان عن الممنوعات بالمناطق الآمنة.`
      );
  } else if (interaction.values[0] === 'safe') {
    embed = new EmbedBuilder()
      .setTitle('🛡️ المناطق الآمنة')
      .setColor(0x2b2d31)
      .setDescription(
`1. يمنع القتل بجميع أنواعه.
2. يمنع الخطف.
3. يمنع إطلاق النار.
4. يمنع الاستفزاز.
5. يمنع بيع أو تداول الممنوعات.
6. يمنع الاحتماء بالمناطق الآمنة إلا حسب النظام.`
      );
  } else if (interaction.values[0] === 'reports') {
    embed = new EmbedBuilder()
      .setTitle('📝 آلية الشكاوي')
      .setColor(0x2b2d31)
      .setDescription(
`1. يجب تقديم دليل كامل وواضح.
2. التصوير إلزامي مع الصوت.
3. يمنع نقص الأدلة.
4. يمنع الكذب أو تلفيق الشكاوي.
5. احترام الإدارة أثناء الشكوى.
6. الشكاوي بدون دليل تُرفض.`
      );
  } else if (interaction.values[0] === 'store') {
    embed = new EmbedBuilder()
      .setTitle('🛒 قوانين المتجر')
      .setColor(0x2b2d31)
      .setDescription(
`1. جميع المشتريات غير مستردة.
2. يمنع مشاركة الحساب.
3. يمنع استغلال المتجر.
4. الالتزام بشروط وأحكام المتجر.
5. الإدارة غير مسؤولة عن أخطاء اللاعب.`
      );
  } else if (interaction.values[0] === 'discord') {
    embed = new EmbedBuilder()
      .setTitle('💬 قوانين الدسكورد')
      .setColor(0x2b2d31)
      .setDescription(
`1. يمنع السب أو الشتم.
2. يمنع الإعلانات بجميع أنواعها.
3. احترام جميع الأعضاء.
4. يمنع الإزعاج أو السبام.
5. الالتزام بتعليمات الإدارة والمشرفين.`
      );
  }

  await interaction.editReply({ embeds: [embed] });
});

// ===============================
// Login (من Railway Variables)
// ===============================
client.login(process.env.BOT_TOKEN);
